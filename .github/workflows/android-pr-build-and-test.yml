name: Android PR Test

on:
  pull_request:
    types:
      - opened

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    # 디렉토리 구조를 출력하는 단계를 추가
    - name: List directories
      run: ls -al

    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'  # 배포판을 adopt로 설정

    - name: Cache Gradle dependencies
      uses: actions/cache@v2
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    # working-directory를 본래의 디렉토리 구조에 맞게 수정
    - name: Build and run tests
      run: ./gradlew clean assembleDebug test
      working-directory: ./MyMindNotes

    - name: Upload test results
      uses: actions/upload-artifact@v2
      with:
        name: test-results
        path: MyMindNotes/app/build/reports/tests

